# name: pdf-compile
# on:
#     workflow_dispatch:
#     pull_request:
#     push:
# jobs:
#     pdf-compile:
#         runs-on: ubuntu-latest
#         container: aergus/latex:latest
#         steps:
#             - name: Checkout repository
#               uses: actions/checkout@v2
#             - name: Install Python Pip
#               run: apt install -y python3-pip && pip install Pygments
#             - name: PDF compile
#               run: latexmk -shell-escape thesis.tex


name: Compile and Publish LaTeX Document

on:
  push:
    branches:
      - main  # Cambia il branch se necessario
    paths-ignore:
      - 'thesis.pdf'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install TeX Live
      run: sudo apt-get install -y texlive-full

    - name: Install Pygments
      run: sudo apt-get install -y python3-pygments

    - name: Compile LaTeX document
      run: latexmk -shell-escape -pdf thesis.tex

    - name: Commit and Push PDF
      run: |
        git config --local user.name "GitHub Actions"
        git config --local user.email "actions@github.com"
        cp thesis.pdf ./
        git add thesis.pdf
        git commit -m "Publish thesis PDF"
        git push origin main

